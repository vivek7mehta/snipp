from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A small repo which will include small reusable utility functions required during ML projects.',
    author='Vivek Mehta',
    license='MIT',
)
